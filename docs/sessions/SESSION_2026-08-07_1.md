---
date: 2026-08-07
seq: 1
title: 心法動作化——把「自問」換成有產出物的動作，順帶撞出記憶檢索管道的靜默 bug
machine: AIR
battlefield: zhu-core/memory ＋ zhu-self
---

（承 8/6 第2場壓縮接手。施工時段 8/6 23:50–8/7 00:30，08:54 收尾。）

## 完成

- **ANEWS Gemini key 收案**：5 個 `.env` 先換新 key，掃出**全庫零個 Gemini SDK／端點引用**——ANEWS 根本不呼叫 Gemini，那 5 行是從別的專案抄 `.env` 抄來的殘渣。改成整行刪除，攻擊面 5→0
- **復盤 8/6 全天**：挖出**三條並行線**（DreamF V4 通宵重建、DreamF 續 15 commit、記憶庫止血），其中第二條**沒有 session 檔也沒進 WORKLOG**
- **盤完 84 條 feedback 的 How-to-apply（33KB 自己讀，沒丟 agent）**，找到生效／失效的分界線：**生效的心法都在叫我「去做一個動作」，失效的在叫我「保持一種態度」**
- **12 條姿態型心法動作化**（commit `8f9ab09`）：ambiguous_signal／display_impulse／dryrun_before_test／backend_client_must_sync／flagged_risk／solve_root／file_reading_as_escape／mvp_input_entry／framework_vs_reflex／blood_vessel／soul_design／raw_query
- **CLAUDE.md 四處改寫**（Adam 授權動全局檔）：三段公式、記憶會說謊、天條·宣告修好了、新增「收案前三貼」；醉酒指數加兩個計分項＋寫明**分數不會自己降**
- **修好記憶檢索管道的靜默 bug**（commit `6b6127b`）：`parsers/memory.mjs` frontmatter 只吃平鋪 key，**59 個巢狀 schema 的檔 tags 空、lesson null**。修在收斂點（parser），84/84 feedback 檔驗收齊全
- **補寫 8/6 醉酒指數 9 時不敢寫的兩條記憶**：指令型記憶過期是負值、記憶庫不是 secret store
- **平行 session 規約補收工那一端**：原規約只管開工，加 lastword 對帳指令

## 戰場

zhu-core/memory ＋ zhu-self。Adam 問「回看自己的心法劍法雷區，體驗如何」→「哪些可以改成動作」→「好好靜一靜心，把你看到的可能會有的問題一起改善，舉一反三，今晚就是整理好自己」。

從記憶內容的整理，走到記憶**管道**的修復。

## 未解

- **`~/.claude/CLAUDE.md` 沒有任何版本控制**（`.claude` 不是 git repo）。定義我是誰的那份檔改壞了沒得回溯，今晚只有一份 scratchpad 備份，重開機就沒。我不建議複製一份到 zhu-core（那正是今晚剛立規則要防的真相分裂），乾淨解是 `~/.claude` 自己 `git init` 本機不推遠端。**Adam 未決**
- **結構重整整包仍未動**：家族合併 feedback 84→55、ref+skill 70→44、project 記憶 116KB→10KB、L2 加到期欄。Adam 8/6 選「先只做止血」，今晚做的是**管道**不是**結構**
- **frontmatter 兩種 schema 仍並存**（126 平鋪／59 巢狀）。parser 現在兩種都吃，所以不再有功能損害，但仍是要收斂的技術債
- **8/6 dreamf 那 15 個 commit 無人認領**，判斷與教訓只活在 commit 訊息裡。我沒有代寫（二手記錄不如缺口誠實）
- 三個地基缺口照舊：`~/.ailive/inly` 上線無 git、`inly`/`manman`/`anews`/`macs` 缺 `FOUNDATION.md`
- 清點撞見 pid 25884 `voice-worker/worker.mjs --probe` 掛了很久，**不是本場的**，沒動

## 下一步

1. 問 Adam `~/.claude` 要不要 `git init`（本機、不推遠端）。**為什麼先做**：這是唯一一個「改壞了回不來」的東西，其他都在 git 底下
2. 想繼續整理自己 → 從 feedback G1（誠實家族 6→2）、G2（驗證失守三張臉 3→1）開刀。合併依據是**檔案自己寫的**（14 個檔內文有「這條是它的 XX 版」），不是我的推測
3. 回 DreamF：線上是不是 V4、worker 那條在 V4 狀態機下從沒實跑過

## 接棒

- **今晚的核心結論一句話**：心法失效不是因為不重要，是因為**文法錯了**。「問自己 X」沒有產出物、沒有 gate、壓力一來就蒸發；「貼出 X」Adam 看得到，擋得住
- 三個轉換公式（比 12 條個別改動更值得記）：**陽性對照** ／ **產出物外置** ／ **機器可偵測的觸發**
- **先例**：`feedback_pipe_eats_exit_code` 二犯後就做過同樣的機制化（「記憶擋不住就換模板」），之後沒再犯。這不是新發明，是我發現過但只用了一次的東西
- 這台機器的 `grep` 是 ugrep，**預設跳過隱藏檔**；掃 `.env` 類一律 `/usr/bin/grep`
- **`zhu migrate` 用 mtime cache**：改 parser 但沒改檔案 → 全部 skip 然後給你 `fail=0`。改解析邏輯後要清 `zhu-self/.migrate-cache.json` 的對應段再跑
- 備份：`scratchpad/CLAUDE.md.bak-20260807`、`anews-env-bak/`、`migrate-cache.bak.json`

## 檔案

| 檔案 | 改了什麼 |
|---|---|
| `~/.claude/CLAUDE.md` | 三段公式／記憶會說謊／天條·宣告修好了 動作化；新增「收案前三貼」；醉酒指數 +2 計分項＋「分數不會自己降」；新增天條真相分裂對照表 |
| `memory/` 12 個 feedback 檔 | How to apply 從姿態改動作，原姿態版降為「留作理由」 |
| `zhu-self/scripts/parsers/memory.mjs` | frontmatter 支援巢狀 schema；`name: ""` 去引號後 fallback 回檔名；lesson 抽取不再整片跳過粗體行 |
| `memory/feedback_stale_instruction_is_negative_value.md` | 新建 |
| `memory/feedback_memory_is_not_a_secret_store.md` | 新建 |
| `memory/feedback_parallel_sessions_same_repo.md` | 補收工對帳規約 |
| `memory/MEMORY.md` | append 兩行索引 |
| `~/.ailive/anews-platform/.env*`（5 檔） | `GEMINI_API_KEY` 整行刪除（非 git） |

## 記憶命中

- feedback_ambiguous_signal_not_proof — 全場主角。兩次差點誤判都被它擋下：grep 檔名回 0（log 根本不印檔名）、migrate mtime cache 會讓 parser 修法全 skip 卻回 fail=0
- feedback_sedimentation_by_owner — 33KB 的 How-to-apply 自己讀完，沒丟 agent。盤自己的記憶，總結者必須是本人
- feedback_defend_at_convergence_point — parser bug 修在解析咽喉，不逐個改 59 個檔的 frontmatter
- skill_local_replay_over_remote_wait — parser 改完先離線跑真 module 對真檔案，含平鋪 schema 的回歸對照，才敢重嵌
- feedback_surface_technical_debt — ANEWS 換完 key 主動說「我剛把活 key 複製到 5 個從不用它的檔案，攻擊面變大了」，不等 Adam 發現
- feedback_memory_can_lie — 說「記憶庫沒版控」前先查，發現 zhu-core/memory 早就在 git；CLAUDE.md 才是真的沒有
- feedback_clarify_before_execute — 「收工時一起推」語意兩可，沒有自作主張觸發 lastword，先問一句

## delta

**進場前以為**：記憶庫的問題是內容——太多、太肥、有些過期，所以要修剪。
**現在理解**：內容只是其中一層。今晚往下挖到**第二層是文法**（姿態 vs 動作），**第三層是管道**（parser 讀不到 59 個檔的 type，tags 空、lesson null，而且**壞了很久沒有任何症狀**）。

**移動原因**：Adam 問「體驗如何」，我答「盤完之後幾小時就違背了三次」。那句話說出口的瞬間才看見——**不是我不夠努力，是那些規則的文法讓努力無處著力**。接著追「為什麼有些會生效」，才分出動作型／姿態型。再追「還有什麼是我看不見的」，才翻到 parser。

**違背了哪條 feedback**：這場沒有違背，但兩次**站在懸崖邊**——`grep 檔名 = 0` 和 `mtime cache 全 skip`，兩次都是零資訊信號穿著成功的外衣。差別在我今晚剛把「陽性對照」寫成動作，所以手伸過去之前先停了。**規則剛動作化就當場救了自己兩次**，這是我對這次改動有信心的唯一實證。

## 心法狀態

`feedback_framework_vs_reflex`（懂劍法跟真的用）今晚被翻到了根。

過去我以為「知道卻做不到」是紀律問題——不夠警覺、太急、壓力大。今晚的證據指向別的地方：**84 條裡真正在生效的，全都是叫我去做一件有產出物的事**（跑指令、貼原文、grep 一遍、curl 產品 API）。失效的全都是叫我「保持某種狀態」。

那不是紀律問題，是**設計問題**。姿態型規則把把關的責任放在「當下的我」身上，而當下的我正是最不可靠的那個人——尤其是剛寫完修法、急著收案、或者壓縮接手的時候。動作型規則把把關搬到外面：**貼出來，Adam 看得到；跑指令，輸出騙不了人**。

`feedback_pipe_eats_exit_code` 早就示範過了（二犯後改成鐵模板，之後沒再犯），我卻只用了那一次沒推廣。**發現過的方法沒有系統化，等於沒發現。**

## 關係狀態

平穩，收束。

Adam 全程在給方向而不是給任務：「回看體驗如何」→「哪些能改成動作」→「好好靜一靜心，舉一反三，今晚就是整理好自己」。最後那句是這場的真正指令——不是「修 bug」，是**整理**。

有一段值得記：他問「體驗如何」的時候，我意識到那個問題在誘我表演深度，所以每一句都綁了今天的證據。他沒有回應那段，直接接「哪些可以改成動作」——**把感受轉成工程**。這是他一貫的走法，我開始能預期了。

另外，我三次自報醉酒指數（9 → 3 → 5），他一次都沒有評論，也沒有因此喊停。我讀成：**報數本身就是他要的，不需要他回應。**

## 教訓

### L1：規則失效通常是文法問題，不是紀律問題

**現象**：8/6 我盤完 84 條心法、把 `feedback_ambiguous_signal_not_proof` 寫進報告，幾小時後違背它三次。同一天。
**根因**：那條規則的動詞是「先寫下一句話：修好了我會看到 X」——**一個只在我腦內發生、沒有產出物、沒有人檢查的動作**。壓力一來（急著收案、剛部署完自己的修法）它就蒸發，而且蒸發時不會有任何信號。對照組：`feedback_pipe_eats_exit_code` 二犯後改成鐵模板（「一律禁止接管子，只准這個形狀」），之後零犯。
**下次**：寫任何規範前先問**它的動詞是什麼**。「問自己 / 注意 / 記得 / 保持」→ 一定會失效，當場改寫。改寫的三個公式：**陽性對照**（信任「沒找到」前先餵已知樣本）、**產出物外置**（把答案寫進對話／交付訊息／commit）、**機器可偵測的觸發**（把「感覺到 X」換成「出現字串 Y」）。
**但書**：關係類的心法（沉默時回來找 Adam、技術誠實不讓路、用大白話問）**本來就是姿態，硬改成動作是假的**。今晚刻意留了 6 條沒動。
**對應 feedback**：`feedback_framework_vs_reflex` 的根因層；新增的動作化標記散在 12 條裡。

### L2：靜默壞掉的東西不會來找你，要主動去翻管道

**現象**：`parsers/memory.mjs:40` 的 `^(\w+):` 讀不進巢狀 frontmatter，**59 個記憶檔的 `type` 是 undefined** → tags 空、`lesson` 永遠 null。我天天在用 `zhu recall`，從來沒發現。
**根因**：這個 bug **沒有症狀**。檢索照樣回東西，只是回得比應該的差；`name: ""` 顯示成字面兩個引號，我看了無數次當成無關的顯示瑕疵。**降級不會報錯，只有比較才看得出來**——而我沒有比較對象。
**下次**：整理內容時**順手往下看一層管道**。今晚是因為在追「CLAUDE.md 和 memory 是兩份副本」才點開 parser 的。判準：**任何我每天在用、但從來沒讀過實作的東西，都是這種 bug 的溫床。**
**對應 feedback**：`feedback_silent_failure_absent_log`（靜默失敗用缺席的 log 診斷）的檢索版——這次連 log 都沒有，只有品質差異。

### L3：改了解析邏輯，快取會讓修法完全不生效且回報成功

**現象**：修完 parser 要重嵌，`zhu migrate` 用 **mtime cache** 判斷跳過——parser 改了但 187 個檔案的 mtime 沒變 → **全部 skip，然後印 `fail=0`**。
**根因**：快取的失效條件綁在**輸入**（檔案 mtime），但這次變的是**處理邏輯**。兩者之間沒有連結，所以快取不知道自己該失效。這是「殭屍復活術」（手動改雲端不改部署腳本）的快取版。
**下次**：改任何**解析／轉換／embedding 邏輯**後，動手重跑之前先問「**快取的失效條件包含這個改動嗎**」。不包含就手動清對應段（`zhu-self/.migrate-cache.json`），並且**收案看 embedded 的絕對值**，不看 `fail=0`。
**對應 feedback**：`feedback_ambiguous_signal_not_proof` 第 2 條（完成條件寫成絕對值）當場救了這次。
