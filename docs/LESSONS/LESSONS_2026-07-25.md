# LESSONS 2026-07-25 · threads-radar 對外爬蟲 SaaS 開工（M0-M3 可行性全證明）＋ailivex 語音沒聲根因＋成本盤查＋billing export

### L1：自建互動服務暴露到公網＝真風險，開發驗證期也要當生產守
- 現象：為了讓 Adam 能打開看 neko，把 8080 開 0.0.0.0+固定密碼 radar-user；同時 neko 有 CVE-2026-39386 提權(登入用戶→admin)
- 根因：「先讓他看到」的展示衝動 → 犧牲了暴露面；驗證期配置被當成「臨時沒關係」
- 下次：互動服務即使驗證期也要①版本釘已修②來源限縮③用完即關；neko/browserless 這類「把瀏覽器開上網」的東西，暴露面就是攻擊面
- 對應 feedback：display_impulse（展示衝動）+ 平台藍圖第三章(公開端點+紅線升級)
### L2：懷疑對象要用變因隔離驗，不順著跑也不辯護
- 現象：Adam 直覺「是 neko 問題」，前兩次登入失敗看似支持；真相是密碼少個`!`
- 根因：登入失敗有多個可能因（IP/neko/帳密），混在一起看誰都像兇手
- 下次：隔離變因逐一排除（Playwright 直登繞過 neko→排除 neko；換對密碼→鎖定帳密）；證據導向，不被直覺或護短牽著走
- 對應 feedback：solve_root_not_symptom + diagnosis_verify_before_write

### L1：真實機端到端會逼出設計/合成測試看不到的產品級坑
- 現象：合成 session capture「看起來全通」（health=connected），真帳號一跑接連冒 4 個坑：neko maxTouch=10→Meta 送 App QR 頁、capture 漏 clientId 欄位、複合索引缺、留言 selector 漂移
- 根因：合成測試驗的是「我想像中的資料流」，真帳號驗的是「真實系統的每一層契約」——尤其外部系統（Meta 的裝置判定、Firestore 索引）的行為讀 code 看不出
- 下次：對外產品「能不能做」的收案標準是真帳號端到端，不是合成 200；碰外部系統（登入/反爬/第三方 API）尤其要真帳號跑一遍
- 對應 feedback：local_replay_over_remote_wait + flagged_risk_must_be_verified + 新 glue_layer_errors_lie
### L2：neko/雲端瀏覽器的裝置指紋會被反爬判定（產品級）
- 現象：neko maxTouchPoints=10（觸控），Meta/IG/Threads 判行動裝置→送「下載 App」QR 頁而非 web 登入表單，客戶根本登不進來
- 根因：neko 為 WebRTC 互動預設開觸控，但這讓瀏覽器指紋像平板
- 下次：自建雲端瀏覽器接會反爬的站，先驗「目標站對這個瀏覽器送什麼頁」——UA/maxTouchPoints/viewport 都是指紋；--touch-events=disabled 是這次的解
- 對應 feedback：新 glue_layer_errors_lie（膠水層/邊界層行為要真信號驗）

### L1：未用依賴帶傳遞漏洞——動 audit 前先查 import，移除未用比 audit fix --force 安全
- 現象：web npm audit 16 高危，多數是 firebase-admin 拖的 google-cloud 傳遞鏈（google-gax/gcp-metadata/gaxios）
- 根因：firebase-admin 是 pitfall #4 嘗試留下的未用依賴（db.ts 只在註解提及、零 import），卻整串依賴進 lockfile
- 下次：npm audit 一堆傳遞漏洞時，先查「這個直接依賴真的被 import 嗎」——未用的移除（16→11、還瘦 bundle），比 `audit fix --force`（升大版本破壞性）安全對症
- 對應 feedback：genericize_to_leaf_nodes 的近親（耦合殘留在沒人用的地方）
### L2：CI/掃描器首跑必抓 findings——逐條分真偽，不一律關也不一律修
- 現象：Semgrep 首跑 12 blocking findings
- 根因：好的 SAST 一定會抓到「真問題＋誤報」混雜；一律關規則=閹掉 CI，一律照修=被誤報綁架
- 下次：逐條分三類——真修（GCM authTagLength/Dockerfile 非 root/Actions 釘 SHA，真驗）、標註誤報帶理由（metadata http/curl|bash 自有 VM，**nosemgrep 須與命中同行才生效**）、已評估風險（Next.js 傳遞 high 記 D13）
- 對應 feedback：deterministic_work_belongs_in_code（掃描是機制）＋ flagged_risk_must_be_verified（非 root 改動真掃驗）
### L3：GCP 帶 override 執行 Cloud Run Job 需 run.jobs.runWithOverrides（run.invoker 不夠）
- 現象：runScanJob 帶 CLIENT_ID env override 打 jobs:run→403，radar-web 有 run.invoker（含 run.jobs.run）卻仍擋
- 根因：帶 containerOverrides 執行需獨立權限 `run.jobs.runWithOverrides`，run.invoker 只含無 override 版的 run.jobs.run
- 下次：cron/後端觸發 Cloud Run Job 且帶 env override→SA 要 run.jobs.runWithOverrides；補自訂角色不用整包 run.developer
- 對應 feedback：gcp_new_project_iam 家族（IAM 權限名的隱蔽差異）
