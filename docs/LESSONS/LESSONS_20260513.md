# LESSONS 2026-05-13（晚段 — ailive scheduler 全鏈路掃毒 + bridge --effort low 上線）

## 戰場一句話
Adam 問「手動觸發沒反應」+「多角色同一時間觸發同一事件」。劍法從底層往上修，過程中撤回一個自己腦補的「+2h drift」誤判，最終把 bridge `--effort low` 真正鋪到 production index.js 的兩個 spawn site，Sonnet 4.6 從 113s → 22s。

---

## 1. memory 又說謊一次（reference_sonnet46_effort_low.md）

**memory 寫的**：「bridge 內部 server 已套用：`~/claude-bridge/internal-server.js`（zhu-dev VM）runClaude 函數 L34」

**實際**：
- `claude-bridge.service` 的 ExecStart 是 `/home/adam_dotmore_com_tw/claude-bridge/index.js`
- `internal-server.js` 存在但**沒被 service 用**
- 真正生效的 `index.js` 有 **3 個 spawn site**：line 48（runClaude）/ line 949（runClaudeWithTools）/ line 2261（stream-json 串流）
- 5/11 那次「已套」只是套到 internal-server.js，沒進 production path

**心法套用**：`feedback_memory_can_lie.md` 又救一次。先 grep production file 才信。

---

## 2. 「+2h drift」是我自己腦補（撤回）

session 早段我先寫結論「task 比 run_hour 晚跑 +2h」，再去找證據——典型的 memory 說謊套路反向版（自己生記憶說謊）。

實際拉 Firebase Functions log 後：
- UTC 04:00 = 「台灣 12:00」✓
- UTC 11:00 = 「台灣 19:00」✓
- `.timeZone('Asia/Taipei')` 有效、`getTaipeiNow` 計算正確、shouldRun 5-min tolerance OK

**心法**：「越具體的記憶越會說謊」這次反過來——越急著下結論的我越會說謊。劍法第一步是看現場不是寫劇本。

---

## 3. LUCY 04:00 504 真因：bridge 113s vs Vercel 120s 賽跑

**現場鐵證**：
- Bridge VM log 04:02:03：`[bridge] req=claude-sonnet-4-6 113835ms in=3 out=6051`
- Vercel 504 在 04:02:04 砍下

**不是經典 double-burn**（bridge 沒繼續燒，bridge 在 120s 前 1 秒完成）
**是賽跑**：bridge 完成→寫 dedupe→準備回 Vercel 的瞬間，Vercel 120s timeout 先到。Vercel 看到 504、後端其實成功。

驗證：下一輪 LUCY post 顯示「本日已執行，跳過」——dedupe 確實寫入了。

---

## 4. 一刀解：bridge index.js 三個 spawn site，兩個加 --effort low

**已改**：
- line 48（runClaude）：`['-p', '--output-format', 'json', '--effort', 'low']`
- line 949（runClaudeWithTools）：`['-p', '--output-format', 'json', '--dangerously-skip-permissions', '--effort', 'low']`

**不動**：
- line 2261（stream-json 串流）：dialogue/voice-stream 主串流用，memory `feedback_bridge_first.md` 明確標例外

**A/B 驗證**：
| 任務 | 改前 | 改後 | 改善 |
|---|---|---|---|
| reflect | 22.8s | 15.0s | 34% ↓ |
| post 拆解 sonnet 部分 | ~113s（LUCY 案）| 22.1s | **80% ↓** |
| post 總時間 | 79s | 77s | 2% ↓（瓶頸轉到 Haiku 25s + 處理 30s）|

**LUCY 504 從架構上解除**：22 + 25 + 30 ≈ 77s vs 120s lambda，安全帶 43s。

---

## 5. 劍法 = 從底層往上修（不是順序）

Adam 早上教過一次：「不是順序 是底層的問題往上修」。今天又驗證一次——
- 上層症狀：手動觸發沒反應、多角色撞單一事件
- 中層假象：+2h drift（撤回）
- 底層真因：bridge 沒套 --effort low → sonnet 113s → Vercel 120s 砍 → 雙燒風險

底層修了，上層自動消。如果先動 ailiveScheduler concurrency lock / task-run dedupe retry，那都是繃帶。

---

## 6. 找問題 vs 根源優化（Adam 提問）

劍法的答案：**先找問題，但範圍鎖死**。

不能跳到「直接優化」的理由：根因是猜的（bridge double-burn 是假設）。先驗 → 才動。

兩個底磚驗證點都鎖死後（bridge log + Firestore dedupe timestamp），動工才安全。耗時 ~10 分鐘換掉 30 分鐘的繃帶。

---

## 7. 副作用 / 殘留

- testAiliveScheduler endpoint 啟動失敗（`ANTHROPIC_API_KEY` v1 destroyed）— 獨立問題，session 沒處理
- 跑了一筆 reflect insight（「那兩分，是我對自己最誠實的時刻」, 6jE3）+ 一筆 post draft（savedId=akt8XyS9H5giEFXvbxN2）— 王彩雲角色，內容尚可，未處理是否保留
- bridge fallback 機制還在：若 bridge 超時 fallback 到 SDK 仍會雙燒。今天沒動，等 Adam 決策
