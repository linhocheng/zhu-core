# LESSONS 2026-05-17b · Atelier E2E 真正跑通

## L1：spawn endpoint 就在那裡，我繞了一圈

- 現象：以為需要建外部 webhook server 才能啟動 Claude Code 子代理
- 根因：沒有先讀 web_server.py 的 route 清單（`@app.post("/api/atelier/tasks/{task_id}/spawn")` 第 3895 行就在那）
- 下次：新系統第一步是 grep routes，不是直接開始設計架構
- 對應 feedback：記憶會說謊——既有的功能要去現場驗，不要只靠假設

## L2：session_token 存在 ~/.hermes/session_token，gateway 活著就有效

- 現象：反覆嘗試從 session 物件取 token，都失敗
- 根因：token 存放位置在 web_server.py line 91，而非 sessions.json
- 下次：curl 401 時先 grep "session_token" 源碼，不要亂猜路徑
- 對應 feedback：動手前先查源碼（2026-05-02 那條）

## L3：Dashboard 空白 = browser 帶舊 token，不是後端壞了

- 現象：後端 API 有 task、狀態 running，但 Dashboard 完全空白
- 根因：WebSocket 連線帶 `window.__HERMES_SESSION_TOKEN__`，這個值在頁面第一次 load 時注入。Gateway 重啟後 token 換新，但 browser 沒有重整
- 下次：gateway 重啟後第一件事是 Cmd+Shift+R，不要從後端找問題

## L4：子代理不需要「我來通知它」，spawn 完 prompt 說清楚就夠

- 現象：花了很多時間設計 webhook → polling → worker 架構
- 根因：把問題複雜化，忘了 Claude Code 是「prompt-driven」，把任務寫進 prompt 就行
- 下次：Adam 問「過度複雜化了嗎」的時候要立刻停下來想
- 對應 feedback：決策前先問：解決問題還是繞開根本問題
