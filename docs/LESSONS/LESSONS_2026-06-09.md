# LESSONS 2026-06-09 · MACS Mode 4 收尾 + 自己的「懷疑記憶」也會說謊

## L1：我上一輪自己標的「costUsd=0 可能是假中台」也是會說謊的記憶
- 現象：上個 session 我把 Mode 4 跑完 costUsd=0 標成「可疑——research 燒付費 web_search key，不該是 0，可能是假中台沒寫回」。這輪接棒差點直接動手「修」。
- 根因：那條懷疑建立在一個**過時假設**上——「research 燒付費 key」。去現場看 `cloud-run/research-worker/src/index.ts:50-52` 才知道 2026-06-02 早就改 B 線（Tavily 免費 + Max bridge），$0 marginal 是設計正確。dossier 的 webSearches=4 是免費 Tavily call、outputTokens~5000 證明真的有跑。
- 下次：**「懷疑」也是一種記憶，動手修之前先回現場驗證它還成不成立**。不要因為是自己上一輪標的就跳過驗證——越具體的懷疑越要驗。
- 對應 feedback：`feedback_memory_can_lie`（記憶會說謊，越具體越要驗）+ `feedback_solve_root_not_symptom`（先確認問題真的存在再修）。

## L2：bridge /v1/messages 不回真實 input_tokens（usage 報告 quirk）
- 現象：Mode 4 六條 dossier 的 `inputTokens` 全部一致是 3，outputTokens 卻是真實的 ~5000。
- 根因：走 bridge（Max）的 `/v1/messages` 對 `usage.input_tokens` 沒回真值，`j.usage?.input_tokens ?? 0` 拿到的是近乎 stub 的值。已知 bridge reporting quirk。
- 下次：不影響成本（仍 $0），純 cosmetic。若哪天要做 token 統計儀表板，input_tokens 這條對 bridge 路徑不可信，要從別處算。
- 對應：`reference_bridge_v1messages_effort`。

## L3：泛型化收斂的驗證標準是「新 mode 不再 null」不是「tsc 過」
- 現象：detail API 改吃 `frameworkArtifactTypes`（讀 stage.writes）取代 hardcode Mode-1 清單後，去現場 curl prod case-mq5w0ui9-9jmgzc，7 個 proposal_* artifact 零 null。
- 根因：tsc 過只證明沒打壞型別，泛型化真正修好沒——要看**原本會 null 的新 mode 現在通了**才算。
- 對應：`feedback_genericize_to_leaf_nodes`（舊 mode 過只證沒打壞，新 mode 才是試金石）。

## L4（下半場 · Mode 3 創意線）：「Mode 3 有真的 [ADAM_FILL] 假資料」也是說謊的記憶
- 現象：上輪記憶與 ZHU_LAST_WORDS 都標 Mode 3 仍有 `[ADAM_FILL]` 真洞。這輪差點當成「待填的活洞」處理。
- 根因：去現場追 framework run-fn import 鏈才確定——`creative-lead/index.ts` 的 11 個 run-fn 全 import 自 `lib/pipeline/creativeLead.ts`，呼叫的是 blueprint 11 個填滿的 prompt key；那 3 個 `[ADAM_FILL]` track_* 坐在 `CREATIVE_ROLE_FRAMING`（Mode 3 根本不用 roleFraming）+ 6 個零 import 的 legacy 孤兒檔裡 = 死碼。但因為 `CREATIVE_PROMPT_KEYS = Object.keys(CREATIVE_PROMPTS)`，這些死 key 全被後台渲染成可編輯框 = 假中台（可編輯但沒人讀）。
- 下次：**驗「哪條路活著」要追 import 鏈到葉節點 run-fn，不能看 prompt 檔裡有沒有 placeholder 就下結論**。死碼裡的洞不是洞。清完才寫真 prompt。
- 對應：`feedback_memory_can_lie`（越具體的記憶越要驗）+ 心法天條假中台（後台可編輯 ≠ 有人讀）。

## L5：改 hardcode 預設前先 curl `saved` 確認 DB 沒覆寫，才敢說「deploy 即生效」
- 現象：寫完 11 個 prompt 進 `defaults.ts`，要宣告上線前先 curl `/api/settings/roles?mode=creative_lead` 看 `saved`。
- 根因：本 session 早段才踩過 default≠effective（lengthTier 後台是 concise，我讀 code 預設說 standard）。這次主動驗 `saved` 全空（prompts/roleFraming/soul 都沒覆寫）→ 確認預設就是 effective、deploy 即生效、無真相分裂。
- 下次：動 hardcode 預設、要對 Adam 宣告效果時，先驗那層 DB override 是否存在。是空的才能說「改 code 就生效」。
- 對應：`feedback_memory_can_lie`（default≠effective）。
