# LESSONS 2026-06-03 · MACS CF524 根治 + e2e 驗通 + UI 設計層

## L1：沙推救了一個 4-worker 大遷移
- 現象：synthesis 掛掉，第一反應是「把剩下 4 個 worker 也搬 Cloud Run」
- 根因：假設「Vercel 平台有問題」，但真正根因是 Vercel→CF 130s edge timeout。兩個診斷的施工範圍差 10 倍。
- 下次：看到 "移平台" 方案前，先問「哪一層 timeout？誰呼叫誰？CF 在哪？」。動手前的沙推（看 BRIDGE_URL 實際值）20 分鐘省了 2 天工。
- 對應 feedback：`feedback_solve_root_not_symptom.md`

## L2：查完現場才知道夥伴已修好
- 現象：我沙推出 C 線方案（bridge-direct HTTPS），結果 Adam 的工程夥伴已經做完了
- 根因：「先看不動手 整個盤過」的指令執行到位，才沒有重複施工或覆蓋別人的工作
- 下次：session 銜接時 `vercel env pull` + `gcloud run services describe` 看 BRIDGE_URL 實際值，不只靠記憶推斷
- 對應 feedback：`feedback_memory_can_lie.md`

## L3：UI「太亂」要看的是哪頁
- 現象：Adam 說 UI 排版太擠，我以為是 dashboard case detail，花時間改了 doc-* CSS layer；結果他指的是 HTML 報告另一頁
- 根因：URL 不同（blob: 開頭），兩個介面是分開的。問題是我沒先問「是哪一頁」
- 下次：收到 UI 反饋前先看 URL，確認目標頁面。有 blob: URL 就是 HTML 報告（renderHtml.ts）；/dashboard/[caseId] 是 case detail。
- 對應 feedback：`feedback_check_admin_before_asking.md`（先自查）
