# LESSONS 2026-08-02 · threads-radar 晚班雙發——F期摩斯切角分析入卡片（evidenceVerified 8/8）＋G期情報站新衣全站上線

### L1：build/deploy 指令必帶絕對路徑，cwd 漂移會在錯的 repo root 留殭屍
- 現象：compound command 後 cwd 落在 repo root，`npx next build`＋`npx vercel --prod` 在 root 誤跑→root 多出 .next/ 殘渣＋.vercel link＋.gitignore 被 vercel 自動改
- 根因：Bash 工具 cwd 跨呼叫持續但被 cd 汙染；build/deploy 這類「在哪跑很重要」的指令沒鎖路徑
- 下次：next/vercel/npm test 一律 `cd <絕對路徑> && ...` 開頭；deploy 前先 `pwd` 級確認
- 對應 feedback：工具滑倒家族（醉酒指數信號）
### L2：LLM 長 JSON 單發呼叫要標配「重抽一次＋失敗診斷」
- 現象：analyze 首跑 parse 不合格寫 failed；本機重放同 prompt 一次即通
- 根因：長結構化輸出（1.5k+ tokens JSON）機率性偶壞是常態不是 bug；單發呼叫沒有重試就把機率暴露給使用者
- 下次：分析類單發 bridge call 標配 for 2 次迴圈＋失敗記 stop_reason/tokens/len/head（診斷欄位讓下次失敗可考古）；批次類（意圖判定 15 篇/掃）本來就下輪自癒可不加
- 對應 feedback：deterministic_work_belongs_in_code（重抽合法、修復必確定性）

### L1：quota/limit 類機制，「per-什麼」比「觸發條件」更致命也更容易記丟
現象：推估八月預算時記得 budget gate 存在（runMonitor.ts:53），把每租戶上限當總帳閘，導出「8/31 擋兩輪」錯誤預測；「調 $100」若照錯框架執行，效果（每戶上限翻倍）與 Adam 意圖（總帳封頂）完全不同。
根因：摘要與記憶保存了「有一道閘」，丟失了作用域；作用域藏在 `tenant.monthlyBudgetUsd ?? global` 的 fallback 寫法和註解裡，行號式記憶看不到它。
下次：引用任何 budget/quota/timeout/limit 前先答「per-什麼？」答不出就回機制本體重讀含註解；要動它之前這步不可跳。INFERRED 標籤一路帶著，直到被 VERIFIED 取代。
對應 feedback：[[feedback_memory_can_lie]]（機制作用域版）、[[feedback_ambiguous_signal_not_proof]]（家族：框架錯了，之後每個信號的解讀都錯）。

### L1：build/deploy/雲端 metadata 指令必帶絕對路徑（cwd 漂移昨日 L1 複驗又踩一半）
- 現象：compound command 後 cwd 漂到 repo root 外，`add-metadata --metadata-from-file=startup-script=neko/startup.sh` 讀不到檔（相對路徑）→ 首推失敗，且同批 `instances start` 卻用**舊** startup 把 VM 開起來（走動態 proxy）
- 根因：Bash 工具 cwd 跨呼叫持續但會被前面的 cd 汙染；「在哪跑很重要」的指令沒鎖絕對路徑
- 下次：next/vercel/gcloud metadata/npm 一律 `cd <絕對路徑> &&` 或旗標帶絕對路徑；改 metadata 後記得 VM 要 stop→start 才生效
- 對應：昨天 SESSION_2026-08-02_1 的 L1 同型，這次是複驗——同型二犯的一半（metadata 那次），醒著才沒釀大錯
### L2：「等人操作」的偵測器超時要按人類真實操作時間設，且要能重生
- 現象：capture.cjs 15 分逾時、開機只跑一次，Adam 登入拖過時（卡對話框＋診斷佔用）→ 登入成功但沒人接
- 根因：超時假設「人 15 分內登完」，但真實登入含意外（deep-link 對話框、換登入頁、求助往返）常超；且逾時即死不重生＝單點
- 下次：等人類互動的 async 元件，超時要寬（或持續偵測）＋逾時後可被外部重觸發；D 期開放給同事前必改（同事比 Adam 更慢更容易卡）
- 對應：feedback_async_worker（韌性）＋feedback_mvp_include_input_entry（人怎麼把輸入送進去這一步最容易被低估）

### L1:天條級雷修好後,同型掃描要跨姊妹平台
- 現象:8/1 才在 ailivex 立「帶人格一律 Sonnet」,同一天 ailive sleep-engine 四個 Haiku 人格 call 原封在跑,拒答落庫兩個月
- 根因:修雷時只掃了發現雷的 repo;姊妹平台(同架構同模式)從不在掃描半徑內
- 下次:立天條/修同型雷時,最後一步 grep 全 ~/.ailive 掃同 pattern(模型名+prompt 形狀),列出姊妹案再收工
- 對應 feedback:feedback_backend_client_must_sync(兩 route 修一邊=沒修)的跨 repo 版
