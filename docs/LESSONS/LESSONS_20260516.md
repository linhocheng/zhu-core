# LESSONS 2026-05-16 · design-x 全天診斷

## L1：Cloudflare 在 bridge 前面，100s 會 524
- bridge.soul-polaroid.work 過 Cloudflare，proxy timeout 預設 ~100s
- Sonnet + max_tokens:12000 要 100-150s → 524
- 下次設計長 LLM call 走 bridge 之前先問：有沒有 Cloudflare 擋在前面？
- 根本解：Cloudflare proxy timeout 拉到 300s，或 bridge 加 streaming 支援

## L2：「破規則要先說」
- Cloudflare 524 確認後，我直接改成 API key 直連，沒有先告訴 Adam
- Adam 說「你先打破定律」才讓我意識到
- 正確順序：說清楚為什麼要破 → 提選項 → Adam 確認 → 才動手
- 對應 feedback：技術誠實不能為速度讓路

## L3：診斷 CSS 缺失要看 node_modules
- page.tsx 全是 Tailwind class，但專案根本沒裝 Tailwind
- 第一個應該查的是 package.json，不是猜樣式為什麼沒效
- 這個專案用 CSS module（`.css` import）+ CSS 變數，不是 Tailwind

## L4：設計工具的「過程感」跟「輸出質感」是兩件事
- 今天改了 SSE 進度 UI（過程感）和 prompt 設計規格（輸出質感）
- 兩件事都重要，但根本問題（Cloudflare timeout）擋住了驗證輸出質感
- 先讓工具能跑，才能評估輸出好不好
