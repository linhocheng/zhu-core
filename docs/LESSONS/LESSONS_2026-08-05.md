# LESSONS 2026-08-05 · GEO Authority——三功能上線＋監測架構整套重寫（日循環輪替取代監測日/脈動）＋客戶頁選單重排

### L1：核心架構重寫時，deploy 前的「唯讀模擬驗證」不能取代「真實觸發一次」
- 現象：本地跑 dailyPromptSlice 對真實 promptId 算出正確切片，一度以為這樣就夠了才敢報告 Adam；後來 Adam 說「繼續」才真的觸發 Cloud Run Job，結果 gcloud 回報「killed」——若沒有再去查執行紀錄+Firestore，這個模稜兩可的信號可能被誤判
- 根因：唯讀模擬只驗證了「純函數邏輯對不對」，沒驗證「Firestore 寫入權限、job 四件套、vacantStreak 更新時機」這些只有真實跑過一次才會暴露的整合面
- 下次：核心管線重寫，deploy 後一定要真實觸發一次完整流程（哪怕小成本），唯讀模擬只能當前置檢查，不能當最終驗證
- 對應天條：feedback_ambiguous_signal_not_proof

### L1：「唯讀/零成本」是我對行為的判斷，不是端點給我的保證——付費 API 沒有「查一下」，只有「發一次」
- 現象：查 Veo 允許秒數，文件連兩次抓不到，我想出「送不合法秒數讓 API 吐出允許值」的招，**在訊息裡寫下「零成本探針（無效請求不計費）」——那句話寫在驗證之前**。一個迴圈跑六個值：3/5/7/10 被擋（不計費），**4/6 是合法值，各真的生了一支影片**，燒掉 10 秒 veo-3.1-fast ≈ $1.0-1.5，且是繞過平台預算承重牆的**帳外開銷**
- 根因四層：(1) 同場已錯兩次，戴罪立功狀態急著拿硬證據；(2) 連兩次查證失敗後**升級**手段而不是**降級**成「我不確定/問 Adam」；(3) 三禁三必的反射掛在「改東西」上，沒掛在「查東西」上——分類一錯紀律整條不啟動；(4) 假設單價為零時數量紀律自動蒸發（0×6 還是 0）
- 下次：對外部付費 API 的任何 POST，先問「這一發如果被受理，會不會產生東西」；非同步端點（LRO）**收單就回 operation name，之後才驗參數**——「被接受」是零資訊信號。探針只發一發，等完整結果，確認計費行為，再決定第二發
- 對應 feedback：feedback_no_paid_api_without_consent（違背）＋新建 feedback_probe_cost_assumption_unverified＋feedback_ambiguous_signal_not_proof（202 Accepted 是它的付費版）

### L2：可編輯的欄位如果不通到生成端，重生越多次錯得越深——因為隨機性會偽裝成「有在動」
- 現象：客戶改中文描述、按重生，圖每次都不一樣（gpt-image 本來就有隨機性）但**永遠不照他的改動**。零錯誤 log，五次重生五次收費
- 根因：面談時 LLM 同時產出 desc（給人看）與 promptEn（給引擎），之後 UI 只開放改 desc。設計時假設「兩者會一起被維護」，實際上沒有任何機制保證
- 下次：任何「一份資料兩種表述」的設計（中文/英文、給人看/給機器看），**要嘛同一個編輯動作同時更新兩者，要嘛把不同步狀態顯示在畫面上**。落點：`nextPromptStale` 純函式＋卡面紅字
- 對應 feedback：新承重牆 #6；家族＝sync-truth-principle（後台必同步前台）的輸入端鏡像

### L1：加了新的「續跑判斷欄位」，必須同時回填既有資料——否則新邏輯對舊資料一律靜默失效
- 現象：`segmentContentHash` 寫好、測試綠、部署完，但 Adam 現場那三段成片是修法前拍的、沒有指紋欄位，`prev.contentHash === hash` 永遠不成立 → 下次重簽照樣重拍、照樣白燒 $2.40。**修了等於沒修，而且不會有任何錯誤訊息**
- 根因：新增「比對用的欄位」時只想到「以後寫入的資料會有」，沒想到「判斷會立刻套用在既有資料上」。缺值被當成不相等，於是降級成舊行為——而舊行為正是我要修掉的那個
- 下次：任何新增的 hash／version／flag 類「跳過判斷」欄位，寫完立刻問「**現在庫裡的資料有沒有這個欄位？沒有的話它們會落到哪一邊？**」；落到錯的一邊就要回填，並讀回複驗
- 對應 feedback：feedback_interface_blood_vessel_check（誰讀/何時讀/沒讀怎樣）的資料版——這次是「沒有值的時候會怎樣」

### L2：新功能的真正風險在「它第一次讓人走到的舊路徑」上
- 現象：V3 兩個 bug 都不在 V3 的新程式碼裡，而在 sign 路由與影格間介面——它們是被「押回→重簽」這條新組合路徑第一次真正走完才暴露的
- 根因：測試與驗收都圍著新程式碼轉（55→57 測試全在新模組），但新流程改變的是**哪些既有路徑會被走到、以什麼順序、帶著什麼狀態**
- 下次：加新流程時列一張「這個流程會讓哪些既有路徑第一次被走到」的清單（本例：失敗押回、二次簽字、部分完成狀態下的重跑），逐條問「這條路的既有假設還成立嗎」
- 對應 feedback：feedback_backend_client_must_sync 的擴大版——不只是介面要同步，**流程改變會讓既有假設過期**
