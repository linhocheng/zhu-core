---
name: 天條 — 確定性的工作用程式，不要丟 LLM
description: 天條級。序列化/解析/驗證/計數/過濾/排序等確定性工作要寫程式保證，連「修 LLM 壞輸出」也要用程式不要再丟 LLM。已寫進全局 CLAUDE.md 永遠生效的紀律
type: feedback
originSessionId: 0f6f6064-d9af-449e-b35e-4001b3d23932
---
> **天條級**（2026-06-04 Adam 升級），同步在 `~/.claude/CLAUDE.md` 永遠生效的紀律。

確定性的工作（序列化、JSON 解析/修復、驗證、計數、過濾、排序、跑公式）一律用程式保證，**不要交給 LLM 去賭**。LLM 只做它無可取代的事：判斷、洞察、生成、創意。

**Why:** MACS「跑很多次但不是每次都成功」的根，是把「產生合法 JSON＝序列化」這件計算機早就解掉的確定性問題，丟給機率模型去做，還在 prompt 裡「拜託模型雙引號記得跳脫」。求模型的那一刻就輸了——把確定性的事塞進不確定的引擎。對照組：MACS 擋假網址是用程式硬過濾（validUrls Set），不是叫模型自律，所以每次都準。

**心態:** 動手前先分類「這一步是判斷，還是計算？」。計算就寫 code。最隱蔽的陷阱是「拿 LLM 去補 LLM 的壞輸出」——例如 JSON 壞了就 re-ask 模型修。那還是用 LLM 做計算機的事，要用程式級 deterministic repair（補括號/修引號逗號/清控制字元）或 tool_use 結構化解碼，靠機制不靠拜託。Adam 2026-06-04 點出時，築自己的藥方 (b) 就踩了這個坑——診斷對一半，藥方又丟回 LLM。

**How to apply:** 看到「請模型輸出 JSON/格式」「壞了再問模型一次」「讓模型算/數/排」就停，問：這能不能用程式 100% 保證？能就寫程式。LLM 的輸出一律當不可信文字，外層用確定性程式 parse/repair/validate。需要 schema 鐵保證走 tool_use，不靠 prompt 哀求。
